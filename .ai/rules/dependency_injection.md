# Dependency Injection Rules

Atlas follows dependency injection.

Objects should receive dependencies.

Avoid constructing services internally.

Depend on interfaces.

Services should remain replaceable.

No hidden dependencies.